import RouteLoadingPanel from "@/app/_components/fallbacks/RouteLoadingPanel";

export default function Loading() {
  return (
    <RouteLoadingPanel
      title="Loading Staff Command Center"
      detail="Building incident feed, heatmap, and broadcast panels."
    />
  );
}
