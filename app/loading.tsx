import RouteLoadingPanel from "@/app/_components/fallbacks/RouteLoadingPanel";

export default function Loading() {
  return (
    <RouteLoadingPanel
      title="Loading GuardianLink"
      detail="Preparing the crisis coordination shell and shared assets."
    />
  );
}
